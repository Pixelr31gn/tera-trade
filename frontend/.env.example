# Copy to .env.local (git-ignored) before running the dashboard.
# Must match the backend's API_KEY (see ../.env / ../backend/.env) exactly,
# or every dashboard API call fails with 401.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
NEXT_PUBLIC_API_KEY=change-me-dev-key
