# Copy to .env (git-ignored) -- read by docker-compose.yml to set up the
# local Postgres container's password. scripts\setup.ps1 generates a real
# random value here automatically; you shouldn't need to touch this by hand.
POSTGRES_PASSWORD=change-me
